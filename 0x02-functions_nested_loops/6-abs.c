#include "main.h"
/**
 * _abs - computes the absolute value of a number
 * @int: data type
 * Return: always 0.
 */
int _abs(int)
{

	if (int > 0)
		return (int * 1);
	else if (int < 0)
		return (int * -1);
	return (0);
}
